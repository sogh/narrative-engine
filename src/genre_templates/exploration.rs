/// Exploration genre template.
///
/// Oriented around discovery and wonder. Descriptive richness,
/// comparison, scale, and novelty.

#[cfg(test)]
mod tests {
    #[test]
    fn placeholder() {
        assert!(true);
    }
}
