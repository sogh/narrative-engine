/// Preview — interactive generation shell for testing grammars and voices.
///
/// Usage: preview --grammars <path> --voices <path>

fn main() {
    println!("preview: not yet implemented");
    println!("Usage: preview --grammars <path> --voices <path>");
}
